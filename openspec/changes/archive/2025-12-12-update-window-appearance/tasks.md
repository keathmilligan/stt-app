## 1. Implementation

- [x] 1.1 Update `src-tauri/tauri.conf.json` to set `resizable: false` on the main window
- [x] 1.2 Update `src-tauri/tauri.conf.json` to set `decorations: false` to remove the title bar
- [x] 1.3 Update `src/styles.css` to apply a dark gray gradient background to the body/root element
- [x] 1.4 Add `data-tauri-drag-region` attribute to the header (`h1`) in `index.html` for window dragging
- [x] 1.5 Style the drag region with appropriate cursor feedback in `src/styles.css`
- [x] 1.6 Test window behavior (fixed size, no title bar, drag region functionality)
